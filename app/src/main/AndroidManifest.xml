<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.fitnessyoga">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AnotherActivity"></activity>
        <activity android:name=".Vai"  />
        <activity android:name=".LungXo" />
        <activity android:name=".Chan" />
        <activity android:name=".Bung" />
        <activity android:name=".Exit" />
        <activity
            android:name=".MainActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu" />
        <activity android:name=".Gym" />
        <activity android:name=".Camera" />
        <activity android:name=".Note" />
    </application>

</manifest>